eureka:
 client:
  register-with-eureka: true
  fetch-registry: true
  service-url:
   defaultZone: http://192.168.49.2:30170/eureka/
 instance:
  hostname: http://localhost
  
services:
 payment:
  endpoints:
   endpoint:
    uri: http://PAYMENT-SERVICE/payment/dopayment
 order:
  endpoints:
   endpoint:
    uri: http://ORDER-SERVICE/order/saveorder
    
